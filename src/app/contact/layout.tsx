export const metadata = {
  title: 'Contact - Stacy Paetz',
  description: 'Get in touch with Stacy Paetz for guest speaking inquiries, appearance requests, and media coaching consultations.',
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
